<% if book.favorited_by?(current_user) %>
    <%= link_to "♥#{book.favorites.count}", book_favorites_path(book), method: :delete ,class: "favorite-btn"%>
<% else %>
    <%= link_to "♥#{book.favorites.count}", book_favorites_path(book), method: :post, class: "unfavorite-btn"%>
<% end %>